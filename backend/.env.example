# Database
DATABASE_URL="postgresql://vicini:changename@localhost:5433/vicini?schema=public"
# --- docker-compose (postgres container) ---
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Server
NODE_ENV=development
PORT=4000
CORS_ORIGINS=http://localhost:5173

# JWT (CHANGE THESE IN PRODUCTION - use `openssl rand -hex 64`)
JWT_ACCESS_SECRET=dev-access-secret-change-me-please
JWT_REFRESH_SECRET=dev-refresh-secret-change-me-please
